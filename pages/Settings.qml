# Add I2P state in the stateGroup
# Add this state after the Mining state in the StateGroup

                State {
                    name: "I2P"
                    PropertyChanges { target: settingsStateView; state: "I2P" }
                } 